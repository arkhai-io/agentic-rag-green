# ChromaDB Configuration
CHROMA_HOST=host.docker.internal
CHROMA_PORT=8000

# Neo4j Configuration
NEO4J_URI=bolt://host.docker.internal:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your_password_here

# OpenRouter for coherence evaluation
OPENROUTER_API_KEY=sk-or-...

# Optional: Download benchmark data at startup (zip URLs)
# PAPERS_URL=https://example.com/papers.zip
# QA_PAIRS_URL=https://example.com/qa_pairs.zip

# Optional: Data directory override
# AGENTIC_ROOT_DIR=/home/agent/data
